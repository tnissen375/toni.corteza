# Copy Image
# - hosts: nc-4-02
#   become: True
#   gather_facts: yes
#   tasks:
#   - name: Copy Image
#     include_role: { name: toni.buildimages, tasks_from: copy }
#   vars:
#     include_image_files:
#         image: "registry.hyconcept.de_ansible_corredor:2.11-infra-0.39"

# Renew cert
# Copy Image
- hosts: nc-4-02
  become: True
  gather_facts: yes
  tasks:
  - name: Certbot certificates
    include_role: { name: toni.certificates, tasks_from: certbot_renew }
  vars:
    letsencrypt_domainname: "{{ certbot_server }}"

    #letsencrypt_endpoint
    #letsencrypt_domainname
    #letsencrypt_email
    